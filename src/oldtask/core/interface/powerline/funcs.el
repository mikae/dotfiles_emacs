;;; package --- Summary
;;; Commentary:
;;; Code:

(defun serika-g/powerline//require ()
  "Require modules for `powerline'."
  (require 'powerline))

(defun init ()
  "Configure `powerline'."
  (serika-g/powerline//require))
