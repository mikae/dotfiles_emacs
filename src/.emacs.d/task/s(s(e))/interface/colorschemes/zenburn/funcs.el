;;; package --- Summary
;;; Commentary:
;;; Code:

(defun init ()
  "Configure `zenburn'."
  ;; (serika-c/eg/add-install :type 'package
  ;;                          :name 'zenburn
  ;;                          :package-list '(zenburn-theme)
  ;;                          :parents      '("base interface install"))


  ;; (serika-c/eg/add-many-by-parents ("base interface install zenburn")
  ;;                                  'require
  ;;                                  (lambda ()
  ;;                                    (require 'zenburn-theme))

  ;;                                  'configure
  ;;                                  (lambda ()
  ;;                                    (load-theme 'zenburn t)))
  )
