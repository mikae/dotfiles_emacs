;;; package --- Summary
;;; Commentary:
;;; Code:
(defun serika-g/rainbow-mode//require ()
  "Require modules for `rainbow-mode'."
  (require 'rainbow-mode))

(defun init ()
  "Configure `rainbow-mode'."
   (serika-g/rainbow-mode//require))
