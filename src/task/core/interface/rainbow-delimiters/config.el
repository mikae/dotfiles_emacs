;;; package --- Summary
;;; Commentary:
;;; Code:

;; Requires
(require 'func-package)

;; Install `rainbow-delimiters'
(serika/package/make-sure-installed 'rainbow-delimiters)

;; Configure `rainbow-delimiters'
(require 'rainbow-delimiters)
